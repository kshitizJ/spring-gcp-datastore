PROJECT_ID: spring-datastore-381909

spring:
  cloud:
    gcp:
      credentials:
        location: classpath:key.json
      datastore:
        namespace: ${DATASTORE_NAME:spring-cloud}
        project-id: ${PROJECT_ID:spring-datastore-381909}
        enabled: true
